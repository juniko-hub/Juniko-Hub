_G.AimbotEnabled = false
_G.AimbotTargetPart = "Head"
_G.AimbotFOV = 60
_G.ShowFovCircle = false
_G.FovCircle = nil

local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Juniko Hub",
    Icon = "crown",
    Author = "by Juniko Team",
    Folder =  "JunikoConfig",
    
    Size = UDim2.fromOffset(580, 460),
    MinSize = Vector2.new(560, 350),
    MaxSize = Vector2.new(850, 560),
    Transparent = true,
    Theme = "Dark",
    Resizable = true,
    SideBarWidth = 200,
    BackgroundImageTransparency = 0.42,
    HideSearchBar = true,
    ScrollBarEnabAimbotSection:Dropdown({
    Title = "Aiming position",
    Desc = "Select the position where the aiming",
    Values = { "Head", "Torso", "Random" },
    Value = { "Head" },
    Multi = false,
    AllowNone = false,
    Callback = function(option) 
        print("Categories selected: " .. game:GetService("HttpService"):JSONEncode(option)) 
    end
})
led = false,
    
    User = {
        Enabled = true,
        Anonymous = false,
        Callback = function()
            WindUI:Notify({
                Title = "Favourite User",
                Content = "It's you? Hi Bro!",
                Duration = 3,
                Icon = "user-star",
            })
        end,
    },
    
    KeySystem = { 
        Key = { "1234" },
        Note = "Juniko Key System",
        URL = "https://link-center.net/2962125/FFIfztvBl86z",
        SaveKey = true,
    },
})

Window:Tag({
    Title = "v1.0.0",
    Icon = "badge-check",
    Color = Color3.fromHex("#00ff00"),
    Radius = 13,
})

Window:EditOpenButton({
    Title = "Open Hub",
    Icon = "crown",
    CornerRadius = UDim.new(0,16),
    StrokeThickness = 2,
    Color = ColorSequence.new(
        Color3.fromHex("#170030"), 
        Color3.fromHex("#310066")
    ),
    OnlyMobile = false,
    Enabled = true,
    Draggable = true,
})

local InfoTab = Window:Tab({
    Title = "Info",
    Icon = "badge-info",
    Locked = false,
})

local Paragraph = InfoTab:Paragraph({
    Title = "Update v1.0.0",
    Desc = " • Add Nothing",
    Color = "Grey",
    Image = "",
    ImageSize = 30,
    Thumbnail = "rbxassetid://108371705582778",
    ThumbnailSize = 80,
    Locked = false,
    Buttons = {
        {
            Icon = "thumbs-up",
            Title = "Cool Update!",
            Callback = function()
                WindUI:Notify({
                    Title = "Really?",
                    Content = "Thanks!",
                    Duration = 3,
                    Icon = "heart",
                })
            end,
        }
    }
})

InfoTab:Divider()

local Button = InfoTab:Button({
    Title = "Copy Discord",
    Icon = "copy",
    Callback = function()
        setclipboard("https://discord.gg/tvoy_server")
        WindUI:Notify({
            Title = "Copied!",
            Content = "You can join to my Discord Server",
            Duration = 3,
            Icon = "copy-check",
        })
    end,
})

Window:Divider()

local MainTab = Window:Tab({
    Title = "Main",
    Icon = "house",
    Locked = false,
})

local AimbotSection = MainTab:Section({ 
    Title = "Aimbot",
    Icon = "locate",
})

AimbotSection:Toggle({
    Title = "AimBot",
    Desc = "Auto aiming",
    Value = false,
    Callback = function(state)
        _G.AimbotEnabled = state
    end
})

AimbotSection:Dropdown({
    Title = "Aiming position",
    Desc = "Select the position where the aiming",
    Values = { "Head", "Torso", "Random" },
    Value = { "Head" },
    Multi = false,
    AllowNone = false,
    Callback = function(option)
        _G.AimbotTargetPart = option
    end
})

AimbotSection:Slider({
    Title = "Fov",
    Desc = "Aiming circle",
    
    Step = 1,
    Value = {
        Min = 20,
        Max = 180,
        Default = 60,
    },
    Callback = function(value)
        _G.AimbotFOV = value
    end
})

AimbotSection:Toggle({
    Title = "Show Fov",
    Desc = "Makes Fov circle visible",
    Value = false,
    Callback = function(state)
        _G.ShowFovCircle = state
    end
})

local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

RunService.RenderStepped:Connect(function(deltaTime)
    if not _G.AimbotEnabled then
        if _G.FovCircle then
            _G.FovCircle.Visible = false
        end
        return
    end
    
    local closestPlayer, closestDistance = nil, math.huge
    local mousePos = Vector2.new(Mouse.X, Mouse.Y + game:GetService("GuiService"):GetGuiInset().Y)
    
    for _, player in pairs(Players:GetPlayers()) do
        if player == LocalPlayer then continue end
        if not player.Character or not player.Character:FindFirstChild("Humanoid") then continue end
        if player.Character.Humanoid.Health <= 0 then continue end
        
        local head = player.Character:FindFirstChild("Head")
        if not head then continue end
        
        local screenPos, onScreen = Camera:WorldToViewportPoint(head.Position)
        if not onScreen then continue end
        
        local distance = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
        
        if distance < closestDistance and distance <= _G.AimbotFOV then
            closestDistance = distance
            closestPlayer = player
        end
    end

    if closestPlayer then
        local targetPart = closestPlayer.Character:FindFirstChild(_G.AimbotTargetPart)
        
        if _G.AimbotTargetPart == "Random" then
            local parts = {"Head", "UpperTorso", "LowerTorso", "LeftUpperArm", "RightUpperArm"}
            targetPart = closestPlayer.Character:FindFirstChild(parts[math.random(1, #parts)])
        end
        
        if not targetPart then targetPart = closestPlayer.Character.Head end
        
        Camera.CFrame = CFrame.new(Camera.CFrame.Position, targetPart.Position)
        
    end

    if _G.ShowFovCircle then
        if not _G.FovCircle then
            _G.FovCircle = Drawing.new("Circle")
            _G.FovCircle.Thickness = 2
            _G.FovCircle.NumSides = 64
            _G.FovCircle.Radius = _G.AimbotFOV
            _G.FovCircle.Color = Color3.fromRGB(220, 30, 30)
            _G.FovCircle.Transparency = 0.75
            _G.FovCircle.Visible = true
            _G.FovCircle.Filled = false
        end

        _G.FovCircle.Position = mousePos
        _G.FovCircle.Radius = _G.AimbotFOV
        _G.FovCircle.Visible = true
    elseif _G.FovCircle then
        _G.FovCircle.Visible = false
    end
end)